#!/usr/bin/python3
def raise_exception():
    # The differance between rasie and except is that except stops program
    # whereas the except, if it was invoked, it will stop the program if it
    # was not the last instruction
    # rasie will give the user a chance to know where excatly the error is
    raise TypeError
